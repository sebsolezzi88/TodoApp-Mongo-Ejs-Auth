<%- include("partials/header") %>

<div class="flex items-center justify-center mt-6">


 <div class="bg-white p-8 rounded-xl shadow-lg w-full max-w-2xl">
        <h1 class="text-3xl font-bold text-emerald-800 mb-6 text-center">Mis Tareas</h1>

        <!-- Botón para agregar nueva tarea -->
        <div class="mb-6 text-right">
            <a href="" class="inline-block bg-emerald-600 text-white font-bold py-2 px-4 rounded-lg hover:bg-emerald-700 focus:outline-none focus:ring-2 focus:ring-emerald-500 transition duration-300">
                ➕ Agregar Nueva Tarea
            </a>
        </div>

        <!-- Mensaje si no hay tareas (inicialmente oculto) -->
        <div class="hidden text-center text-gray-500 mb-6">
            <p class="text-lg">Aún no tienes tareas. ¡Haz clic en "Agregar Nueva Tarea" para empezar!</p>
        </div>

        <!-- Contenedor de tareas -->
        <div id="tasks-container" class="space-y-4">
            
          

        </div>
        <!-- Fin Contenedor de tareas -->
    </div>
    </div>

<%- include("partials/footer") %>
