<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo App Mongo</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-gray-100">
    <nav class="bg-emerald-800 p-3 flex justify-between items-center">
    <header class="text-white text-xl">TodoApp</header>
    
    <% if (user) { %>
      <div class="flex items-center space-x-4">
        <span class="text-white">Hola, <%= user.username %> </span>
        <a href="/user/logout" class=" text-white px-3 py-1 rounded-lg ">
          Logout
        </a>
      </div>
    <% } else { %>
      <div>
        <a href="/user/login" class="text-white px-3 py-1 rounded-lg ">
          Login
        </a>
      </div>
    <% } %>
  </nav>